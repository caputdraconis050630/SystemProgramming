#include <stdio.h>

int main() {
	int i = 1000;
	printf("i is %d in decimal format. \n", i);
	printf("i is 0%o in octal format.\n", i);
	printf("i is 0x%x in hexadecimal format. \n", i);
	return 0;
}
